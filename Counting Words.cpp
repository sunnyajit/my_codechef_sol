#include <iostream>
using namespace std;
// code written by sunny
int main() {
    int T;
    cin >> T; 
    while (T--) {
        int N, M;
        cin >> N >> M; 
        int totalWords = N * M;
        cout << totalWords << endl;
    }

    return 0;
}
